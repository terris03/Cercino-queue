// Firebase disabled to prevent console errors
// Using localStorage only for reliable operation
// To re-enable Firebase, ensure your database exists and has proper rules

export const app = null;
export const analytics = null;
export const db = null;
